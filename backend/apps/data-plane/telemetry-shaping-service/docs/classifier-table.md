| Nhóm metric                  | Ví dụ key                                                                                                                                                 | Source               | Lane                            | Nên vào feature/window? | Lý do                                                                                           |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- | ------------------------------- | ----------------------- | ----------------------------------------------------------------------------------------------- |
| Identity                     | `node.hostname`, `node.os_product`, `node.primary_ipv4`, `container.name`, `container.image`, `container.service_name`                                    | windows, docker      | `context`, `snapshot`           | Không                   | Dùng để hiển thị và join context, không có giá trị chuỗi thời gian                              |
| Inventory/Capacity tĩnh      | `node.logical_cpu_count`, `container.memory_limit_bytes`, `node.memory_total_bytes`, `node.ssd_total_space_gb`, `node.battery_design_capacity_mwh`        | windows, docker, lhm | `context`, `snapshot`           | Thường không            | Là baseline/capacity, ít đổi; useful để normalize feature khác nhưng không cần window trực tiếp |
| Runtime state text           | `container.status`, `container.state`, `container.health_status`, `node.primary_nic_status`                                                               | docker, windows      | `snapshot`, `stream`, `context` | Chưa, hoặc encode sau   | Quan trọng cho dashboard và state transition, nhưng chưa nên đưa raw text vào ML window         |
| Resource usage gauge         | `node.cpu_usage_pct`, `node.memory_used_pct`, `container.cpu_usage_pct`, `container.memory_used_pct`, `service.cpu_usage_pct_sum`                         | windows, docker      | `stream`, `window`, `snapshot`  | Có                      | Đây là core signal cho monitoring và feature analytics                                          |
| Throughput/rate              | `node.network_rx_bytes_sec`, `node.disk_write_bytes_sec`, `container.network_tx_bytes_sec`, `node.tcp_segments_retransmitted_rate`, `node.ssd_write_rate` | windows, docker, lhm | `stream`, `window`              | Có                      | Rất hợp cho spike/trend/anomaly                                                                 |
| Saturation/pressure          | `node.cpu_queue_length`, `node.disk_queue_length`, `node.memory_page_faults_rate`, `container.pid_count`, `node.process_count`, `node.thread_count`       | windows, docker      | `stream`, `window`, `snapshot`  | Có                      | Đây là nhóm tín hiệu mạnh cho incident prediction                                               |
| Thermal/Power                | `node.cpu_temperature_c`, `node.cpu_package_power_w`, `node.gpu_core_load_pct`, `node.gpu_core_clock_mhz`, `node.ssd_temperature_c`                       | lhm                  | `stream`, `window`, `snapshot`  | Có                      | Quan trọng cho health/risk, đặc biệt phần hardware                                              |
| Storage health/wear          | `node.ssd_life_pct`, `node.ssd_percentage_used_pct`, `node.ssd_available_spare_pct`, `node.ssd_power_on_hours`, `node.ssd_power_on_count`                 | lhm                  | `snapshot`, `window`            | Có chọn lọc             | Hợp cho risk/trend dài hạn hơn là chart realtime dày                                            |
| Battery health               | `node.battery_charge_pct`, `node.battery_degradation_pct`, `node.battery_remaining_capacity_mwh`                                                          | lhm                  | `snapshot`, `stream`, `window`  | Có chọn lọc             | Quan trọng nếu chạy laptop/edge node, nhưng không phải lane ưu tiên cho server cố định          |
| Service rollup               | `service.container_count`, `service.running_container_count`, `service.memory_used_bytes_sum`                                                             | docker               | `snapshot`, `stream`, `window`  | Có                      | Rất tốt cho dashboard cấp service và anomaly ở level service                                    |
| Cumulative lifetime counters | `node.ssd_data_read_gb`, `node.ssd_data_written_gb`                                                                                                       | lhm                  | `snapshot`                      | Chưa                    | Giá trị tuyệt đối tăng dần, chưa hữu ích nếu không derive delta/rate                            |
| Count inventory phụ          | `container.port_binding_count`, `container.mount_count`, `container.network_count`                                                                        | docker               | `context`, `snapshot`           | Không                   | Metadata vận hành, không phải signal realtime                                                   |
