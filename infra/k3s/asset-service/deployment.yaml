apiVersion: apps/v1
kind: Deployment
metadata:
  name: asset-service
  namespace: ar-monitoring-dev
  labels:
    app.kubernetes.io/name: asset-service
    app.kubernetes.io/part-of: ar-monitoring
spec:
  replicas: 1
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app.kubernetes.io/name: asset-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: asset-service
        app.kubernetes.io/part-of: ar-monitoring
    spec:
      containers:
        - name: asset-service
          image: ghcr.io/example/ar-monitoring-asset-service:latest-main
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 4002
          envFrom:
            - configMapRef:
                name: asset-service-config
            - secretRef:
                name: asset-service-secrets
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
